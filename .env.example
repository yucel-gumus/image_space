# Server-only (Vercel Environment Variables — never use VITE_* for secrets)
AI_API_URL=https://python-backend-270384591051.europe-west3.run.app
# Same value as gateway CLIENT_API_KEYS (server-side only; Vercel + local .env for vite proxy)
GATEWAY_CLIENT_API_KEY=your-gateway-client-key-here